import test from'node:test';import assert from'node:assert/strict';import{applyCommerceEvent}from'../../../marketplace/commerce/lifecycle.mjs';import{createArtifactReceipt,verifyArtifactReceipt}from'../../../marketplace/artifacts/receipt.mjs';test('paid fixture grants once, verifies artifact, and refund revokes',()=>{let state={entitlement:{status:'PENDING'}};state=applyCommerceEvent(state,{type:'checkout.session.completed'});assert.equal(state.entitlement.status,'ACTIVE');const bytes=Buffer.from('universal-package');const receipt=createArtifactReceipt({bytes,productVersionId:'PV',runtime:'UNIVERSAL',buildId:'B',provenanceReceiptId:'E'});assert.equal(verifyArtifactReceipt(bytes,receipt),true);state=applyCommerceEvent(state,{type:'charge.refunded',full_refund:true},{full_refund:true});assert.equal(state.entitlement.status,'REVOKED');});