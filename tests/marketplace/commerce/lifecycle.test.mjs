import test from'node:test';import assert from'node:assert/strict';import{applyCommerceEvent}from'../../../marketplace/commerce/lifecycle.mjs';test('payment grants and duplicate provider handling is policy-independent',()=>assert.equal(applyCommerceEvent({entitlement:{}},{type:'checkout.session.completed'}).entitlement.status,'ACTIVE'));test('full refund revokes perpetual access',()=>assert.equal(applyCommerceEvent({entitlement:{status:'ACTIVE'}},{type:'charge.refunded',full_refund:true},{full_refund:true}).entitlement.status,'REVOKED'));test('partial refund does not silently revoke',()=>assert.equal(applyCommerceEvent({entitlement:{status:'ACTIVE'}},{type:'charge.refunded',full_refund:false},{}).entitlement.status,'ACTIVE'));test('admin revoke requires actor and reason',()=>assert.throws(()=>applyCommerceEvent({}, {type:'admin.revoke'}),/REASON_AND_ACTOR/));