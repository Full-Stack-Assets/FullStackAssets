import test from'node:test';import assert from'node:assert/strict';import{verifyBackupManifest,RECOVERY_ORDER}from'../../../marketplace/operations/backup-manifest.mjs';test('backup verifier requires complete ordered recovery evidence',()=>{const m={canonical_snapshot_hash:'a',db_backup_hash:'b',artifact_inventory_root_hash:'c',catalog_snapshot_hash:'d',audit_high_water_mark:'e',timestamp:'t'};assert.equal(verifyBackupManifest(m).status,'PASS');assert.deepEqual(RECOVERY_ORDER,['CANON','MARKETPLACE_DB','ARTIFACTS','ENTITLEMENTS','CATALOG']);});