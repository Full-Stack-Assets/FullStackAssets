import test from'node:test';import assert from'node:assert/strict';import{candidateReadiness}from'../../../marketplace/publisher/candidates.mjs';test('candidate readiness orders blockers',()=>assert.deepEqual(candidateReadiness({schema_valid:true,provenance_complete:true,evaluation_status:'MISSING',offer_status:'MISSING',runtime_build_status:'MISSING'}),{state:'NEEDS_EVALUATION',blockers:['EVALUATION_REQUIRED','RUNTIME_BUILD_REQUIRED','OFFER_REQUIRED']}));