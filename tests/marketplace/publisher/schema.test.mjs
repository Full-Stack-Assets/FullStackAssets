import test from 'node:test';import assert from 'node:assert/strict';import{readFileSync}from'node:fs';const sql=readFileSync('marketplace/db/migrations/003_publisher_studio.sql','utf8');for(const t of ['publisher_memberships','commercial_candidates','canon_change_proposals','publication_reviews','runtime_build_jobs','publisher_audit_events'])test(`schema creates ${t}`,()=>assert.match(sql,new RegExp(`CREATE TABLE ${t}`)));test('publisher scoped and canon proposal is downstream only',()=>{assert.match(sql,/publisher_id TEXT NOT NULL REFERENCES publishers/);assert.match(sql,/canonical_ref TEXT NOT NULL/);assert.doesNotMatch(sql,/UPDATE\s+(roles|skills|canonical)/i);});