import test from'node:test';import assert from'node:assert/strict';import{approvePublication}from'../../../marketplace/publisher/publication-service.mjs';function repo(hash='abc'){return{transaction:async f=>f({getPublicationReview:async()=>({status:'PENDING',product_version_id:'PV',expected_version_hash:'abc',packet:{}}),getProductVersion:async()=>({id:'PV',product_id:'P',canonical_hash:hash}),insertPublicationRecord:async()=>{},markReview:async()=>{},setAvailability:async()=>{},appendOutbox:async()=>{}})}}test('payload bound approval rejects stale version',async()=>await assert.rejects(()=>approvePublication(repo('def'),{reviewId:'R',reviewer:{id:'H',human_authority:true},expectedVersionHash:'abc'}),/STALE/));test('automation cannot self approve',async()=>await assert.rejects(()=>approvePublication(repo(),{reviewId:'R',reviewer:{id:'BOT'},expectedVersionHash:'abc'}),/HUMAN_AUTHORITY/));