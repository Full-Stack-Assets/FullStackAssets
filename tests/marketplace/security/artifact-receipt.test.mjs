import test from'node:test';import assert from'node:assert/strict';import{createArtifactReceipt,verifyArtifactReceipt}from'../../../marketplace/artifacts/receipt.mjs';test('artifact receipts are deterministic and detect mutation',()=>{const b=Buffer.from('abc');const r=createArtifactReceipt({bytes:b,productVersionId:'PV',runtime:'UNIVERSAL',buildId:'B',provenanceReceiptId:'E'});assert.equal(r,createArtifactReceipt({bytes:b,productVersionId:'PV',runtime:'UNIVERSAL',buildId:'B',provenanceReceiptId:'E'}));assert.equal(verifyArtifactReceipt(Buffer.from('abd'),r),false);});