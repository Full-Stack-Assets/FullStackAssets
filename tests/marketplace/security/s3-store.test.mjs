import test from'node:test';import assert from'node:assert/strict';import{S3ArtifactStore}from'../../../apps/marketplace-api/src/artifacts/s3-store.mjs';test('s3 adapter uses immutable content-addressed keys',async()=>{const calls=[];const client={headObject:async()=>{throw new Error('missing')},putObject:async x=>calls.push(x),signGetObject:async()=>({url:'x'})};const s=new S3ArtifactStore({client,bucket:'b'});const meta={productId:'P',version:'1.0.0',runtime:'UNIVERSAL',sha256:'a'.repeat(64),filename:'x.zip'};const r=await s.putImmutable(meta,Buffer.from('x'));assert.match(r.key,/products\/P\/1\.0\.0\/UNIVERSAL\/a{64}\/x\.zip/);assert.equal(calls.length,1);});