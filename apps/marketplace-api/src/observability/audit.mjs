const FORBIDDEN=['token','secret','card','artifact_payload','raw_sensitive'];export function sanitizeAuditEvent(event){const out={};for(const[k,v]of Object.entries(event))if(!FORBIDDEN.some(x=>k.toLowerCase().includes(x)))out[k]=v;for(const k of ['actor_id','action','object_ref','result','correlation_id'])if(!out[k])throw new TypeError(`AUDIT_${k.toUpperCase()}_REQUIRED`);return out;}
