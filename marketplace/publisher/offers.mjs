export function validateOffer(o={}){if(!o.license_policy_id)throw new TypeError('LICENSE_POLICY_REQUIRED');if(!['FREE','ONE_TIME','MONTHLY','ANNUAL','TEAM','ENTERPRISE','CUSTOM'].includes(o.offer_class))throw new TypeError('OFFER_CLASS_INVALID');if(!Number.isInteger(o.amount_minor)||o.amount_minor<0)throw new TypeError('AMOUNT_MINOR_INVALID');if(o.offer_class==='FREE'){if(o.amount_minor!==0)throw new Error('FREE_AMOUNT_MUST_BE_ZERO');}else if(!/^[A-Z]{3}$/.test(o.currency??''))throw new TypeError('CURRENCY_INVALID');return true;}
export function offerChangeRequiresHuman(a,b){if(!a&&b&&b.offer_class!=='FREE')return true;if(!a||!b)return false;return a.amount_minor!==b.amount_minor||a.currency!==b.currency||a.license_policy_id!==b.license_policy_id||a.redistribution_rights!==b.redistribution_rights||a.enterprise_only!==b.enterprise_only;}
