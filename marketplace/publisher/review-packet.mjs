export function buildReviewPacket(c={}){const required=['product','product_version','canonical_refs','canonical_hashes','change_severity','risk','evaluation_summary','rights_provenance','runtime_status','offer_license_diff','requested_action'];for(const k of required)if(c[k]===undefined)throw new TypeError(`REVIEW_PACKET_${k.toUpperCase()}_REQUIRED`);return Object.freeze({...c,created_at:c.created_at??new Date().toISOString()});}
