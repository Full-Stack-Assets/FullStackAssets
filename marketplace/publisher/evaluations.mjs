export function validateEvaluationRecord(r={}){if(!r.id||!r.product_version_id)throw new TypeError('EVALUATION_ID_REQUIRED');if(!Array.isArray(r.fixtures)||!r.fixtures.length)throw new TypeError('EVALUATION_FIXTURES_REQUIRED');if(r.critical_failures>0)throw new Error('EVALUATION_CRITICAL_FAILURE');if(r.provenance_complete!==true)throw new Error('EVALUATION_PROVENANCE_REQUIRED');return true;}
export function deriveTrustState(records=[],runtimeDistributions=[]){if(!records.length)return 'SPECIFIED';const passed=records.filter(r=>{try{return validateEvaluationRecord(r);}catch{return false;}});if(!passed.length)return 'TESTED';const runtimeOk=runtimeDistributions.some(r=>r.compatibility_state==='VERIFIED'&&r.artifact_hash);if(!runtimeOk)return 'TESTED';const prod=passed.some(r=>r.production_observation_days>0&&r.production_evidence===true);const pilot=passed.some(r=>r.pilot_evidence===true);return prod?'PRODUCTION_PROVEN':pilot?'PILOTED':'VERIFIED';}
