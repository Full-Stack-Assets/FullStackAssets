export const RECOVERY_ORDER=['CANON','MARKETPLACE_DB','ARTIFACTS','ENTITLEMENTS','CATALOG'];export function verifyBackupManifest(m={}){for(const k of ['canonical_snapshot_hash','db_backup_hash','artifact_inventory_root_hash','catalog_snapshot_hash','audit_high_water_mark','timestamp'])if(!m[k])throw new Error(`BACKUP_${k.toUpperCase()}_MISSING`);return{status:'PASS',recovery_order:RECOVERY_ORDER};}
