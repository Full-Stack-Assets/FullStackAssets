import{renameSync,rmSync,mkdirSync,cpSync,existsSync}from'node:fs';import{join,dirname}from'node:path';import{createHash}from'node:crypto';export function hashBytes(b){return createHash('sha256').update(b).digest('hex')}export function promoteCatalog(candidateDir,liveDir,{validate}){if(!validate(candidateDir))throw new Error('CATALOG_VALIDATION_FAILED');const backup=`${liveDir}.backup`;if(existsSync(backup))rmSync(backup,{recursive:true,force:true});if(existsSync(liveDir))renameSync(liveDir,backup);try{mkdirSync(dirname(liveDir),{recursive:true});renameSync(candidateDir,liveDir);if(existsSync(backup))rmSync(backup,{recursive:true,force:true});}catch(e){if(existsSync(liveDir))rmSync(liveDir,{recursive:true,force:true});if(existsSync(backup))renameSync(backup,liveDir);throw e;}return{status:'PROMOTED'};}
